import {mkdtemp,readFile as readSecret} from 'node:fs/promises';import os from 'node:os';import path from 'node:path';
export async function setup(createApp){const dataDir=await mkdtemp(path.join(os.tmpdir(),'moyun-test-'));const server=createApp({dataDir});await new Promise(r=>server.listen(0,'127.0.0.1',r));const base='http://127.0.0.1:'+server.address().port;const r=await fetch(base+'/api/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({password:(await readSecret(path.join(dataDir,'teacher-password'),'utf8')).trim()})});if(!r.ok)throw Error('測試登入失敗');const Cookie=r.headers.get('set-cookie').split(';')[0];return {server,base,Cookie};}
