# 原型驗證紀錄

日期：2026-09-15

## 自動測試

- node --check server.mjs：通過。
- node --check public/app.js：通過。
- npm test：1 個整合測試通過，覆蓋非法教材、代碼格式、未授權拒絕、答案隱藏、選項範圍、重複作答、公布鎖定、舊頁作答拒絕、填空判分、便利貼公布、CSV 資料權限及結課。

## Chrome 實際操作

- 首頁：確認宣紙底色、青綠山水、Q 版書生、三份教材可見。
- 教師開課成功，以不同學生分頁加入測試教室。
- 學生投票「水的流動」，教師顯示 1 人、對應統計與暱稱。
- 教師切至選擇題，學生分頁同步。學生選擇「夕陽西下」並成功送出。
- 教師公布答案，學生端正確答案可見且按鈕停用。
- 填空「更上一層樓」、開放回應與便利貼均成功送出。
- 便利貼公布後，學生端可見暱稱與內容。
- 學生分頁重新整理後，保留教室、暱稱身分及既有填空答案，已修正初版返回加入表單問題。
- 匯出按鈕觸發，介面顯示「已下載 5 筆作答紀錄」；下載檔案讀回另受本機工具限制，不以提示當作落盤驗證。
- 手機 viewport 指定 375×812；Chrome 實際 DOM width=341、scrollWidth=341，無整頁水平溢位。實際截圖確認投票按鈕垂直排列。驗證後已重設 viewport。

## 尚未完成的驗證

- 原生結課確認視窗使瀏覽器控制工具逾時，因此 UI 結課結果尚未驗證；後端結課已有自動測試。
- 教材編輯與 JSON 匯入未完成瀏覽器端往返測試。
- YouTube 與外部資源尚未做逐站播放測試。
- 沒有實體手機、跨區網及多人負載測試。

## 下一個里程碑

補齊編輯器 UI 測試、持久化教室與帳號，再按功能對照表實作手寫拍照、配對與記憶牌卡。正式部署應獨立驗收。

## 第二版：上傳、手寫與上限移除

- npm test：2 個整合測試通過，涵蓋 PDF/PPTX/PPT/PNG 真實上傳、逐頁轉換、圖片 HTTP 讀回、手寫提交、教師筆跡權限、132 頁教材、121 名加入者與 105 間額外教室。
- 視覺檢查發現 PowerPoint 中文未渲染，改用明確 Fontconfig 系統中文字型目錄後，重新轉換並確認標題及內文完整可見。
- Chrome UI：兩頁 PDF 範例走真實上傳 API，成功轉頁；於第 1、2 頁中間插入手寫互動，頁序正確。
- Chrome UI：教師用原子筆與毛筆書寫，分享後學生畫面可見兩條不同筆跡；學生用原子筆、鉛筆、螢光筆及橡皮擦作答，復原/重做通過，教師端顯示收到的 PNG，截止後提交停用。
- Chrome 自動化本機選檔因擴充功能未允許 file URLs 而被擋，未更改設定；瀏覽器範例匯入及後端真實檔案上傳皆通過，不把兩者冒稱實體檔案選擇器端到端成功。
- 尚未實測硬體觸控筆筆壓及大型並發；已實測滑鼠速度變化的毛筆筆跡。

## 四專家修正候選版

完整紀錄與未完成項目見 [專家修正驗收](專家修正驗收.md)。此版尚未正式部署。
