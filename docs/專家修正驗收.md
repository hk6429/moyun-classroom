# 四專家建議修正對照（發布候選版）

## 已實作且已有自動／畫面證據

| 建議 | 實作 | 證據 |
|---|---|---|
| 課堂與作答保存 | 原子檔案保存、教師續課列表 | 重啟整合測試；預覽伺服器重啟後學生身分／作答畫面保留 |
| 文字草稿 | 依教室／頁次／學生保存 | Chrome重新整理後未送出文字仍在 |
| 完整備份 | 教材JSON內含圖片；還原重建素材 | 實際API往返及圖片bytes一致 |
| 未作答／回饋 | 完整學生×互動題目報告、個別評語、作答次數 | 4列報告中3列未交測試；Chrome顯示教師回饋與解析 |
| 權限 | 教師登入、圖片授權、跨站檢查、登出撤銷 | 未登入401、異班圖片403、學生控制403、登出舊權杖401 |
| 手寫保存 | PNG與筆畫分開保存，按需還原 | 筆畫往返一致，無效筆畫400 |
| 文字替代手寫 | 同題文字作答 | Chrome送出成功並保留 |
| 規模 | 移除固定頁數／人數門檻，增加資源保護 | 132頁、121人與105間回歸；121並行小型PNG本機測試全部收到 |

## 已實作，仍有人工／環境驗收待辦

- 學習目標、成功條件、教材全文、分層提示、答案解析；範例出口題已對齊目標。
- 教師名冊在線狀態、鎖定與移除成員、評語草稿。
- 授課頁面目錄、下一活動與縮圖跳頁。
- 教師影片播放／暫停／指定秒數、學生重新同步。Chrome只確認入口及嵌入框，實際播放呈黑畫面，不能宣稱播放同步已驗證。
- 手寫筆模式及重新整理後筆畫可編輯。實體筆壓、手掌誤觸、iPad／Chromebook及讀屏尚未實測。
- 匯入取消、失敗檔重試、整體等待期限。容器取消與資源上限需在可用Docker環境測試。
- 素材用量與可還原回收區；使用中素材禁止移除。正式營運保留／清除期限須由管理者設定與執行，目前不自動永久刪除資料。

## 阻擋正式部署

- 此機Docker daemon無法連線；OrbStack UI啟動與orbctl start未成功。隔離Dockerfile已備妥，但未build／執行成功。
- 尚無指定可用Linux主機、正式網域與部署帳號。已向使用者詢問，尚待回覆。
- 預覽使用可信fixture的本機轉檔模式，只綁127.0.0.1；程式阻止設定PUBLIC_ORIGIN時啟用此未隔離模式。
- 公開HTTPS入口未建立，不能把GitHub push或本機預覽稱為正式上線。

## 測試紀錄

- 完整回歸4/4通過（DEVELOPMENT_LOCAL_CONVERTER=1，CREATE_RATE_LIMIT=200，獨立DATA_DIR，SOFFICE_PATH指向已安裝引擎）。
- 筆畫分離儲存後追加上傳聚焦測試通過；node --check及git diff --check通過。
- 121並行加入／小型PNG提交／狀態讀取：365請求皆200，121份收到；p95 106ms，max107ms。同一本機HTTP，不代表校園實網或大型作品。
- 新增bundle測試、security測試、load-check腳本；原始師生權限測試保留。

## 發布方式

修正在expert-fixes-20260915分支與獨立worktree；原main與4178站維持原版。完成阻擋項目後，按deploy/README.md部署、健康檢查與回復流程。
