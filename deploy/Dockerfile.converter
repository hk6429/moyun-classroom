FROM node:22-bookworm-slim
RUN apt-get update && apt-get install -y --no-install-recommends libreoffice-impress poppler-utils fonts-noto-cjk && apt-get clean && rm -r /var/lib/apt/lists/*
COPY deploy/convert.mjs /opt/convert.mjs
ENV HOME=/tmp
WORKDIR /work
ENTRYPOINT ["node","/opt/convert.mjs"]
